# Neon MCP Database Access Rules
  - Connect Neon MCP only to a database project named `RecipesDB` in my Neon account

# Database migrations
 - If you need to modify the database, follow the standard Drizzle workflow: change drizzle schema file --> generate migration --> migrate to neon DB
 
# Modular components
Split app pages and source code into simple modular components (avoid long complex files).